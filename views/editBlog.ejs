<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('./partials/head') %>
    <title>Edit Blog</title>
    <link rel="stylesheet" href="/styles/main.css">
    <style>
        body {
            background: linear-gradient(to right, #536475, #206bbb);
            font-family: 'Arial', sans-serif;
        }

        .container {
            background-color: rgba(255, 255, 255, 0.95);
            padding: 30px;
            margin-top: 40px;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        h1 {
            color: #fff;
            text-align: center;
            margin-bottom: 20px;
            font-size: 2.5rem;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
            background: linear-gradient(to right, #206bbb, #536475);
            -webkit-background-clip: text;
            color: transparent;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            font-size: 1.1rem;
            color: #333;
        }

        .form-control {
            background-color: #f9f9f9;
            color: #333;
            border-radius: 8px;
            border: 1px solid #ccc;
            padding: 10px;
            font-size: 1rem;
            width: 100%;
        }

        .form-control:focus {
            border-color: #206bbb;
            box-shadow: 0 0 5px rgba(32, 107, 187, 0.5);
        }

        .alert {
            background-color: #f8d7da;
            border-color: #f5c6cb;
            color: #721c24;
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 8px;
            text-align: center;
        }

        .btn {
            background-color: #206bbb;
            color: #fff;
            border-radius: 8px;
            padding: 10px 20px;
            font-size: 1rem;
            border: none;
            cursor: pointer;
            width: 100%;
        }

        .btn:hover {
            background-color: #155a8a;
        }

        small {
            font-size: 0.9rem;
        }

        .form-group a {
            color: #206bbb;
            text-decoration: none;
        }

        .form-group a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="container">
        
        <h1>EDIT YOUR THOUGHTS</h1>

        <!-- Form to edit the blog -->
        <form method="post" action="/blog/edit/<%= blog._id %>" enctype="multipart/form-data">
            <!-- Title Input -->
            <div class="form-group">
                <label for="title">Title</label>
                <input type="text" name="title" id="title" class="form-control" value="<%= blog.title %>" required>
            </div>

            <!-- Body Textarea -->
            <div class="form-group">
                <label for="body">Body</label>
                <textarea name="body" id="body" rows="10" class="form-control" required><%= blog.body %></textarea>
            </div>

            <!-- Cover Image Input -->
            <div class="form-group">
                <label for="coverImage">Cover Image</label>
                <input type="file" name="coverImage" id="coverImage" class="form-control">
                <small>Current Image: <a href="<%= blog.coverImageURL %>" target="_blank">View</a></small>
            </div>

            <!-- Submit Button -->
            <div class="form-group">
                <button type="submit" class="btn">Save Changes</button>
            </div>
        </form>
    </div>
    
    <%- include('./partials/scripts') %>
</body>

</html>
